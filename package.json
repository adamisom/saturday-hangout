{
  "name": "saturday-hangout",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
